# Phase 1-2 deps. Loose lower bounds for now (Python 3.13 needs recent wheels).
# After Phase 1 runs clean, we'll `pip freeze > requirements-lock.txt` to capture
# the resolved versions for real reproducibility.

# Core data + ML
pandas>=2.2.3
numpy>=2.1.0
scikit-learn>=1.5.2
xgboost>=2.1.2
imbalanced-learn>=0.12.4

# Experiment tracking (Phase 2). 2.17+ has solid Python 3.13 support.
mlflow>=2.17.0

# Notebooks + EDA
jupyterlab>=4.2.5
matplotlib>=3.9.2
seaborn>=0.13.2

# Serving (Phase 3)
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0

# Tests + linting
pytest>=8.3.3
httpx>=0.27.0  # FastAPI TestClient uses httpx under the hood
ruff>=0.6.9
